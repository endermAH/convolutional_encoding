#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

const char *INPUT = "1001000"

const char *G1 = "1"
const char *G2 = "11"

void main() {
  char mem_buffer = NULL
  int mem_buffer_length = 0;

  mem_buffer_length = max(strlen(G1),strlen(G2));

  for( int i = 0; i <= strlen(INPUT); i++ ) {

  }
}
